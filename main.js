"use strict";

{


  for (let i = 0; i < 10; i++) {
    //乱数を生成(0以上1未満)
    // const n = Math.random();
    //2以下の乱数
    const n = Math.random() * 3;
    console.log(`${i}: ${Math.floor(n)}`);
  }
  

}